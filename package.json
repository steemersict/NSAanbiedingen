{
  "name": "nsaanbiedingen",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "build:backend": "cd backend && pyinstaller backend.spec --clean",
    "build:rename": "node scripts/rename-sidecar.js",
    "build:sidecar": "npm run build:backend && npm run build:rename",
    "tauri": "tauri",
    "tauri:dev": "npm run build:sidecar && tauri dev",
    "tauri:build": "npm run build:sidecar && tauri build"
  },
  "dependencies": {
    "@astrojs/tailwind": "^6.0.2",
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-dialog": "^2.0.0",
    "@tauri-apps/plugin-fs": "^2.0.0",
    "@tauri-apps/plugin-shell": "^2.0.0",
    "nanostores": "^0.10.0",
    "react": "^18.2.0",
    "react-dnd": "^16.0.1",
    "react-dnd-html5-backend": "^16.0.1",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@astrojs/react": "^4.4.2",
    "@tauri-apps/cli": "^2.0.0",
    "astro": "^4.5.0",
    "tailwindcss": "^3.4.0",
    "typescript": "^5.3.0"
  }
}
